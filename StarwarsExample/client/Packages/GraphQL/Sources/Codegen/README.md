# Swift API Code Generation

`Codegen.swift` demonstrates generating GraphQL sources directly with the `GraphQLCodegen` Swift API. It creates a
`Configuration` for the example's schema and documents, then runs `Codegen` with that configuration.

The application uses `GraphQLCodegenPlugin`; this executable demonstrates the alternative Swift API integration.
